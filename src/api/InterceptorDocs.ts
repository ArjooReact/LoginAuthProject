// What is Axios Interceptor?
// Ans:Axios interceptors are the function that axios call for every request .
// Means whenever axios call its own requestAnimationFrame, for every requestAnimationFrame, interceptor
// objerve those requestAnimationFrame, give provision them with two methods, to intercept those request
// or transform these data before request happen or after getting response or also we are able to TransformStream, manipulate
// those req and response and also taking their useAnimatedValue.